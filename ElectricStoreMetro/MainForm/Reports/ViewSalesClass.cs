using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using System.Threading.Tasks;
using System.Data.SqlClient;

namespace ElectricStoreMetro.MainForm.Reports
{
    public class ViewSalesClass
    {
        //public DataSet ViewAllSales()
        //{
        //    var ds = new DataSet();
        //    using (var con = new SqlConnection(DBConnection.cn))
        //    {
        //        using (var cmd = new SqlCommand(Queries.ViewSales, con))
        //        {
        //            con.Open();
        //            var da = new SqlDataAdapter(cmd);
        //            da.Fill(ds);
        //        }

        //    }
        //    return ds;
        //}

        //public DataSet ViewAllSalesForReport()
        //{
        //    var ds = new DataSet();
        //    using (var con = new SqlConnection(DBConnection.cn))
        //    {
        //        using (var cmd = new SqlCommand(Queries.ViewSales, con))
        //        {
        //            con.Open();
        //            var da = new SqlDataAdapter(cmd);
        //            da.Fill(ds);
        //        }

        //    }
        //    return ds;
        //}
    }
}
